export enum ResponseCodes {
  NOT_FOUND = 404,
  NOT_AUTHORIZED = 401,
  BAD_REQUEST = 400,
  SUCCESS = 200,
  SERVER_ERROR = 500,
  CONFLICT = 409,
}
export enum AccountTypes {
  SAVING = "SAVINGS",
  CURRENT = "CURRENT",
  SALARY = "SALARY",
}